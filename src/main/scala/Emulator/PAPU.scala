package Emulator

/** Audio Processing Unit class, permits to generate the sounds/music for the emulator.
  * Uses registers, several channels (exactly four: pulse, triangle, noise and delta modulation), frame counters and a mixer.
  */
class PAPU {
  def reset: Unit = {

  }
  def exWrite(address: Int, value: Byte): Unit = {

  }
}
